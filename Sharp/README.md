- Tests were made with C# NUnit Framework and Selenium.
- Local Parallelization on method level
- Tried to implement SpecFlow tests but failed to do so(Not a lot of information is available on the Internet).

